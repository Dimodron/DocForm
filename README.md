# DocForm — помощник для работы с юридическими документами

Небольшое десктоп-приложение на Python, созданное мной для моей жены.  
Ей было неудобно одновременно:

- читать длинные юридические документы;
- держать рядом шаблон Word;
- вручную копировать выводы в нужные места отчёта.

Поэтому я сделал инструмент, который объединяет всё в одном окне:  
слева исходный документ, справа — поля для отчёта, а результат автоматически подставляется в DOCX-шаблон.

---

## 🚀 Готовая версия программы

Всё уже собрано — ничего устанавливать не нужно.

👉 **[DocForm v1.0.0 ](https://github.com/Dimodron/DocForm/releases/tag/v1.0.0)**

Распакуйте архив в любую удобную для вас папку и перейдите в `dist` а там запустите файл `main.exe`  

## Возможности программы

### ✔️ Просмотр документов  
Слева можно открыть:

- PDF  
- Word (doc / docx)  
- Excel (xls / xlsx)  
- Текстовые файлы (txt / log / md)

Текст можно выделять, копировать, вставлять.

---

### ✔️ Поля для отчёта (справа)

Можно создавать собственные поля:

- `text` (однострочное поле)
- `multiline` (многострочный текст)
- `checkbox` (галочка)

Для каждого поля задаётся:

- внутреннее имя (используется в шаблоне как `{{NAME}}`);
- отображаемая метка;
- тип.

Функции управления полями:

- добавление,
- изменение,
- удаление,
- перемещение вверх/вниз,
- автоматическое приведение внутренних имён к ВЕРХНЕМУ РЕГИСТРУ.

---

### ✔️ Шаблоны полей (профили)

Можно создавать несколько разных профилей — например:

- шаблон для договора аренды,
- шаблон для договора поставки,
- шаблон для искового заявления.

Каждый профиль содержит:

- свой набор полей,
- свой DOCX-шаблон.

Все профили и файлы шаблонов хранятся в папке **`storage/`**, лежащей рядом с программой.

---

### ✔️ DOCX-шаблон  
Каждый профиль может подключить свой корпоративный шаблон `.docx`.

В нём используются плейсхолдеры вида:

```text
{{NAME}}
{{ORGANIZATION}}
{{COMMENT}}
